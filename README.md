# Saudi-solar-data-preprocessing-pca
Data preprocessing and dimensionality reduction pipeline for solar dataset. The notebook inspects data quality, handles missing values, detects outliers using the IQR method, applies Min-Max and Z-score normalization, analyzes feature correlations, and performs PCA to reduce dimensional variables into principal components for analysis and modeling.
